{% extends 'base.html.twig' %}

{% block title %}Guinot-immobilier{% endblock %}

{% block body %}
    <div class = "jumbotron">
        <br> GUINOT IMMOBILIER:</br>
        <br> ACCESS LOCATION & VENTE D'APPARTEMENTS</br>
    </div>

-----------------------------------------------------
<div class="container">
      
{# Liste de ce que j'aimerai afficher#}
<div class = "row">
     <div class="col-6">
          {% for immobilier in immobiliers %}     
               <resume>
               {# Faisant une interpollation#}
               <h5>categorie / {{ immobilier.titre }}</h5>
               <p> Date de publication: {{ immobilier.createdAt | date('d/m/Y')}}</p>
               <div class = "content">
                    <img src="{{ immobilier.photo}}" alt=""  align="left">
                    <p align="justify"> {{ immobilier.description}}   </p> 
                    <a href="{{ path('index.affich', {'id': immobilier.id })}}" class="btn btn-primary"> Consulter</a>  
               </div>          
               </resume>
               <hr> </hr>
          {% endfor %} 
     </div>
     <div class="col-6">
          {% for immobilier in immobiliers %}     
               <resume>
               {# Faisant une interpollation#}
               <h5>categorie / {{ immobilier.titre }}</h5>
               <p> Date de publication: {{ immobilier.createdAt | date('d/m/Y')}}</p>
               <div class = "content">
                    <img src="{{ immobilier.photo}}" alt=""  align="left">
                    <p align="justify"> {{ immobilier.description}}   </p> 
                    <a href="{{ path('index.affich', {'id': immobilier.id })}}" class="btn btn-primary"> Consulter</a>  
               </div>          
               </resume>
               <hr> </hr>
          {% endfor %} 
     </div>

</div>

 <div class="pagination">
        {{ knp_pagination_render(immobiliers) }}
    </div>

</div>

{% endblock %}
